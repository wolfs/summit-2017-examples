ext.timestamp = new Date().time

subprojects {
    apply plugin: 'java-library'
    sourceCompatibility = 8
    targetCompatibility = 8

    version = "${baseVersion}-${timestamp}"

    repositories {
        jcenter()
    }

    dependencies {
        testCompile "junit:junit:4.12"
    }
}
